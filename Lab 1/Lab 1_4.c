#include<stdio.h>
#include<math.h>

int main()
{
    int a,b,s,area;
    a=6;
    b=8;
    area=a*b;
    printf("Area of the rectangle is:%d\n",area);
    s=2*(a+b);
    printf("Perimeter of the rectangle is:%d\n",s);
    return 0;
}
